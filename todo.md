# todo
+ switch keyboards based on language
+ undo/redo (cannot re-use browsers undo/redo system)
- decouple key bindings and functionality (no keys in language modules, jsut functionality)
- create separate functions for each behaviour, instead of doing everything in constructor
  set default options to include behaviour, that can be overridden
- autoinsert closing brace/bracket (in javascript)
- autoindent next line on enter
- investigate @lezer (from codemirror) for highlight/parse of content
- add html preview option
- highlight current html element in preview, based on cursor position
- open preview in separate window
- switch preview between mobile/desktop mode
- allow multiple previews in a single window
- add api to change preferences
- add optional menu to change preferences
